version: "3.8"

services:
  # 如需使用容器内置 MySQL，取消下面 db 服务的注释
  # db:
  #   image: mysql:5.7
  #   container_name: pan_db
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: pan_root_123
  #     MYSQL_DATABASE: cccpan
  #     MYSQL_USER: cccpan
  #     MYSQL_PASSWORD: cccpan_123
  #   volumes:
  #     - db_data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"

  app:
    build: .
    container_name: pan_app
    restart: unless-stopped
    ports:
      - "5858:80"
    volumes:
      - ./:/var/www/html
      - ./assets/avatars:/var/www/html/assets/avatars
    # 如启用了上面的 db 服务，取消下面 depends_on 注释
    # depends_on:
    #   - db

# 如启用了上面的 db 服务，取消下面 volumes 注释
# volumes:
#   db_data:
